/*chrome.runtime.onMessage.addListener(
	function (message, response_callback) {
		if (message[action] === "RunContentScript") {
			chrome.tabs.executeScript({
				file: 'content_script.js'
			});
		}
	}
);*/

function lol(){
	return 41;
}